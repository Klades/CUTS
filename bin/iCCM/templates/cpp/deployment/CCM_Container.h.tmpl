// -*- C++ -*-

//=============================================================================
/**
 *  @file         ${arch_name}_CCM_Container.h
 *
 *  $$Id$$
 *
 *  Code generated by iCCM skeleton project generator that is include
 *  in the CUTS (http://cuts.cs.iupui.edu) and developed by:
 *
 *      Software Engineering & Distributed System (SEDS) Group
 *      Indiana Univesity-Purdue University Indianapolis
 *      Indianapolis, IN 46202
 *      USA
 */
//=============================================================================

#ifndef _${arch_name.upper()}_CCM_CONTAINER_H_
#define _${arch_name.upper()}_CCM_CONTAINER_H_

#include "cuts/arch/ccm/CCM_Container_T.h"

#include "${arch_name}_CCM_Container_Strategy.h"
#include "../ccm/${arch_name}_CCM_Servant.h"

// Forward decl.
class ${arch_name}_CCM_Instance_Handler;

/**
 * @class ${arch_name}_CCM_Container
 *
 * CCM container implementation for the ${arch_name} architecture.
 */
class ${arch_name.upper()}_CCM_DEPLOYMENT_HANDLERS_Export ${arch_name}_CCM_Container :
  public CUTS_CCM_Container_T <${arch_name}_CCM_Container,
                               ${arch_name}_CCM_Instance_Handler,
                               ${arch_name}_CCM_Container_Strategy,
                               ${arch_name}_CCM_Servant>
{
public:
  /// Type definition of the base type.
  typedef
    CUTS_CCM_Container_T <${arch_name}_CCM_Container,
                          ${arch_name}_CCM_Instance_Handler,
                          ${arch_name}_CCM_Container_Strategy,
                          ${arch_name}_CCM_Servant> base_type;

  /**
   * Initializing constructor.
   *
   * @param[in]           handler           Pointer to instance handler
   * @param[in]           poa               PortableServer for container
   */
  ${arch_name}_CCM_Container (${arch_name}_CCM_Instance_Handler * handler, ::PortableServer::POA_ptr poa);

  /// Destructor.
  virtual ~${arch_name}_CCM_Container (void);
};

#if defined (__CUTS_INLINE__)
#include "${arch_name}_CCM_Container.inl"
#endif

#endif  // !defined _${arch_name}_CCM_CONTAINER_H_
