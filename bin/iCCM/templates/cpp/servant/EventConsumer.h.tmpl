// -*- C++ -*-

//=============================================================================
/**
 *  @file         ${arch_name}_EventConsumer.h
 *
 *  $Id$
 *
 *  Code generated by iCCM skeleton project generator that is include
 *  in the CUTS (http://cuts.cs.iupui.edu) and developed by:
 *
 *      Software Engineering & Distributed System (SEDS) Group
 *      Indiana Univesity-Purdue University Indianapolis
 *      Indianapolis, IN 46202
 */
//=============================================================================

#ifndef _ICCM_${arch_name.upper()}_EVENTCONSUMER_H_
#define _ICCM_${arch_name.upper()}_EVENTCONSUMER_H_

#include "cuts/iccm/servant/EventConsumer.h"

#include "${arch_name}_EventsS.h"
#include "${arch_name}_svnt_export.h"

namespace iCCM
{

// Forward decl.
class ${arch_name}_Servant;

/**
 * @class ${arch_name}_EventConsumer
 *
 * Servant implmentation of the ::Components::${arch_name}::EventConsumer
 * object that is defined in ${arch_name}_Events.idl. This class also
 * inherits from iCCM::EventConsumer so that it can be used in the Servant_T
 * class, which implements common behavior for all CCM servant objects.
 */
class ICCM_${arch_name.upper()}_SVNT_Export ${arch_name}_EventConsumer :
  public virtual EventConsumer,
  public virtual ::POA_Components::${arch_name}::EventConsumer
{
public:
  /// Default constructor.
  ${arch_name}_EventConsumer (void);

  /// Destructor.
  virtual ~${arch_name}_EventConsumer (void);

  /// INSERT CODE HERE

private:
  /// INSERT CODE HERE
};

}

#if defined (__CUTS_INLINE__)
#include "${arch_name}_EventConsumer.inl"
#endif

#endif  // !defined _ICCM_${arch_name}_EVENTCONSUMER_H_
