// -*- C++ -*-

//==============================================================================
/**
 *  @file       ${arch_name}_Servant.h
 *
 *  $Id$
 *
 *  Code generated by iCCM skeleton project generator that is include
 *  in the CUTS (http://cuts.cs.iupui.edu) and developed by:
 *
 *      Software Engineering & Distributed System (SEDS) Group
 *      Indiana Univesity-Purdue University Indianapolis
 *      Indianapolis, IN 46202
 */
//==============================================================================

#ifndef _ICCM_${arch_name.upper()}_SERVANT_H_
#define _ICCM_${arch_name.upper()}_SERVANT_H_

#include "cuts/iccm/servant/Servant.h"

#include "${arch_name}_EventConsumer.h"
#include "${arch_name}_Publisher.h"
#include "${arch_name}_Publisher_Table.h"

namespace iCCM
{

/**
 * @class ${arch_name}_Servant
 *
 * Base class for all ${arch_name} servant objects. This class enables
 * the container to send commands to the servant, such as
 * domain-specific configuration information.
 */
class ICCM_${arch_name.upper()}_SVNT_Export ${arch_name}_Servant :
  public Servant
{
public:
  /// Trait definitions for iCCM::Servant_T object.
  typedef ${arch_name}_EventConsumer eventconsumer_type;
  typedef ${arch_name}_Publisher publisher_type;
  typedef ${arch_name}_Publisher_Table publisher_table_type;

  /// Default constructor.
  ${arch_name}_Servant (const char * name);

  /// Destructor.
  virtual ~${arch_name}_Servant (void);
};

}

#if defined (__CUTS_INLINE__)
#include "${arch_name}_Servant.inl"
#endif

#endif  // !defined _ICCM_${arch_name}_SERVANT_H_
