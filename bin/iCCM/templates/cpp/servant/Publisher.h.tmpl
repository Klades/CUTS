// -*- C++ -*-

//=============================================================================
/**
 *  @file         ${arch_name}_Publisher.h
 *
 *  $Id$
 *
 *  Code generated by iCCM skeleton project generator that is include
 *  in the CUTS (http://cuts.cs.iupui.edu) and developed by:
 *
 *      Software Engineering & Distributed System (SEDS) Group
 *      Indiana Univesity-Purdue University Indianapolis
 *      Indianapolis, IN 46202
 */
//=============================================================================

#ifndef _ICCM_${arch_name.upper()}_PUBLISHER_H_
#define _ICCM_${arch_name.upper()}_PUBLISHER_H_

#include "cuts/iccm/servant/Publisher.h"

#include "${arch_name}_EventsC.h"
#include "${arch_name}_svnt_export.h"

namespace iCCM
{

/**
 * @class ${arch_name}_Publisher
 *
 * The base class for all ${arch_name} publishers that are integrated into
 * the iCCM architecture. In essence, this class is an adapter class that
 * allows a ${arch_name} publisher to integrate with the iCCM architecture.
 */
class ICCM_${arch_name.upper()}_SVNT_Export ${arch_name}_Publisher :
  public Publisher
{
public:
  /// Default constructor.
  ${arch_name}_Publisher (void);

  /// Destructor.
  virtual ~${arch_name}_Publisher (void);

  /**
   * Connect an event consumer to this publisher. This reference should
   * be narrowed to a ::Components::${arch_name}::EventConsumer object.
   *
   * @param[in]           consumer          Referene to event consumer.
   */
  virtual void connect (::Components::EventConsumerBase_ptr consumer);

  /// Disconnect the consumer.
  virtual ::Components::EventConsumerBase_ptr disconnect (void);

  /// INSERT CODE HERE

protected:
  /// The consumer connected to this publisher.
  ::Components::${arch_name}::EventConsumer_var consumer_;

  /// INSERT CODE HERE
};

}

#if defined (__CUTS_INLINE__)
#include "${arch_name}_Publisher.inl"
#endif

#endif  // !defined _ICCM_${arch_name.upper()}_PUBLISHER_H_
