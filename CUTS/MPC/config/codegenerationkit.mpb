// $Id$

project {
  after += CodeGenerationKit
  libs  += CodeGenerationKits

  includes += $(CUTS_ROOT)/contrib/CCF \
              $(CUTS_ROOT)/contrib/utility

  libpaths += $(CUTS_ROOT)/contrib/CCF/CCF
}
