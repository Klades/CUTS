Variables:
	int id_;

Columns: 
	int LF1.uid;
	int LF2.uid; 
	
Relations: 
	LF1.uid->LF2.uid;
	
On LF1:
if(key == "uid")
{
  this->id_ = this->id_ + 1;
  int32_var * ivar = dynamic_cast <int32_var *> (var);
  ivar->value (this->id_);
}
		
On LF2:
if(key == "uid")
{
  this->id_ = this->id_ + 1;
  int32_var * ivar = dynamic_cast <int32_var *> (var);
  ivar->value (this->id_);
}

