<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<cruisecontrol>
  <!-- pull in the environment configuration -->
  <property environment="env" toupper="no"/>

  <!--
    Specification for the vc8 build
    -->
  <project name="CUTS-vc8"
           requireModification="true"
           buildafterfailed="false">
    <property name="build.type" value="vc8" />

    <listeners>
      <currentbuildstatuslistener file="logs/${project.name}/status.txt"/>
    </listeners>

    <bootstrappers>
      <svnbootstrapper localWorkingCopy="${env.CUTS_ROOT}" />
    </bootstrappers>

    <modificationset quietperiod="60">
      <svn LocalWorkingCopy="${env.CUTS_ROOT}"
           checkExternals="true"
           useLocalRevision="true" />
    </modificationset>

    <schedule>
      <ant anthome="apache-ant-1.7.0"
           buildfile="${env.CUTS_ROOT}/integration/autobuild.build"
           target="build.noupdate"
           time="0200">
        <property name="mpc.type" value="${build.type}" />
      </ant>
    </schedule>
  </project>

  <!--
    Specification for the vc8 force only build
    -->
  <project name="CUTS-vc8-forcebuild"
           requireModification="false"
           forceOnly="true">
    <property name="build.type" value="vc8" />

    <listeners>
      <currentbuildstatuslistener file="logs/${project.name}/status.txt"/>
    </listeners>

    <bootstrappers>
      <svnbootstrapper localWorkingCopy="${env.CUTS_ROOT}" />
    </bootstrappers>

    <modificationset quietperiod="60">
      <svn LocalWorkingCopy="${env.CUTS_ROOT}"
           checkExternals="true"
           useLocalRevision="true" />
    </modificationset>

    <schedule>
      <ant anthome="apache-ant-1.7.0"
           buildfile="${env.CUTS_ROOT}/integration/autobuild.build"
           target="build.noupdate">
        <property name="mpc.type" value="${build.type}" />
      </ant>
    </schedule>
  </project>
</cruisecontrol>
