// $Id$

project : udm_component {
  libs  += CUTS_CHAOS_DSML
  after += CUTS_CHAOS_DSML

  dynamicflags += CHAOS_HAS_DLL
  macros       += UDM_DYNAMIC_LINKING

  includes += $(CIAO_ROOT)/CCF
  libpaths += $(CIAO_ROOT)/CCF/CCF
}
