// $Id$

project : aceexe, cuts_base {
  install = $(CUTS_ROOT)/bin

  specific (wix) {
    install_location = bin
  }
  
  specific (prop:windows) {
    subsystem = 0
  }

  specific (vc11) {
    subsystem = NotSet
  }
}
