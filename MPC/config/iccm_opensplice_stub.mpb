// $Id$

project : splicelib, ccm_stub, requires_opensplice {
  libs  += iCCM_OpenSplice_stub
  after += iCCM_OpenSplice_stub

  specific (prop:microsoft) {
    // I really would like to remove this configuration parameter, but
    // we need it for the time being since there are multiple symbols
    // defined and we cannot figure out how to avoid this!

    link_options += /FORCE:MULTIPLE
  }
}
