project (iCCM_Portico_Compiler) : iccm_compiler {
  exename   = iccm_portico

  verbatim (gnuace, local) {
    ifeq ($(TAO_IDL_PREPROCESSOR),)
      CPPFLAGS += -DTAO_IDL_PREPROCESSOR=\\\"$(strip $(CXX))\\\"
    else
      CPPFLAGS += -DTAO_IDL_PREPROCESSOR=\\\"$(subst \\,\\,$(TAO_IDL_PREPROCESSOR))\\\"
    endif

    VPATH = $(TAO_ROOT)/TAO_IDL $(TAO_ROOT)/TAO_IDL/driver

    ifeq ($(static_libs_only), 1)
      ifneq ($(with_ld), macosx)
        LDFLAGS += -static
      endif
    endif
  }

  specific (prop:microsoft) {
    subsystem = 0
    macros += _SCL_SECURE_NO_WARNINGS
  }

  specific (vc11) {
    subsystem = NotSet
  }

  Source_Files {
    Compiler.cpp
    Context.cpp
    Stub_File.cpp
    Event_ObjectClass.cpp
    Event_ObjectClass_Factory.cpp
    Event_Traits.cpp
  }

  Header_Files {
  }
}
